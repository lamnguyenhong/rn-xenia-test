import React from 'react'
import { Text, View } from 'react-native'
import { ProfileScreenPayloadProps } from './ProfileScreen.props'

export const ProfileScreen = (props: ProfileScreenPayloadProps) => {
  return (
    <View>
      <Text>
        Profile Screen
      </Text>
    </View>
  )
}